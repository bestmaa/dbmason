services:
  db-control:
    build: .
    image: ${DBMASON_IMAGE:-dbmason:local}
    init: true
    restart: unless-stopped
    ports:
      - "${DBMASON_BIND_ADDRESS:-127.0.0.1}:${PORT:-3000}:3000"
    environment:
      DATABASE_URL: file:/app/data/control-plane.db
      PAYLOAD_SECRET: ${PAYLOAD_SECRET:?Set PAYLOAD_SECRET in .env}
      CONNECTION_ENCRYPTION_KEY: ${CONNECTION_ENCRYPTION_KEY:?Set CONNECTION_ENCRYPTION_KEY in .env}
      DBMASON_PUBLIC_URL: ${DBMASON_PUBLIC_URL:?Set DBMASON_PUBLIC_URL in .env}
      DATABASE_HOST_ALLOWLIST: ${DATABASE_HOST_ALLOWLIST:-}
      DBMASON_SOURCE_URL: ${DBMASON_SOURCE_URL:-}
    volumes:
      - db-control-data:/app/data
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    cpus: ${DBMASON_CPU_LIMIT:-1.0}
    mem_limit: ${DBMASON_MEMORY_LIMIT:-384m}
    pids_limit: 256
    stop_grace_period: 20s
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://127.0.0.1:3000/api/health').then(r=>{if(!r.ok)process.exit(1)})"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s

volumes:
  db-control-data:
